const authRoutes = require('./auth');
const ppeItemsRoutes = require('./ppeItems');
const driversRoutes = require('./drivers');
const transactionsRoutes = require('./transactions');
const reportsRoutes = require('./reports');

module.exports = {
  authRoutes,
  ppeItemsRoutes,
  driversRoutes,
  transactionsRoutes,
  reportsRoutes
};
